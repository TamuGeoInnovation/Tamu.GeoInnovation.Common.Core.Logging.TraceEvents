namespace USC.GISResearchLab.Common.Diagnostics.TraceEvents
{
    public enum DatabaseEvents
    {
        Connecting,
        Connected,
        Opening,
        Opened,
        Closing,
        Closed,
        Disconnecting,
        Disconnected
    }
}
